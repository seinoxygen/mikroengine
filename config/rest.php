<?php

$config['format'] = 'json';

// The table name that will host all keys and limits.
$config['limit_table'] = 'rest';

// Column on the table.
$config['table_key'] = 'api_key';

// Key name usedn in posts and gets so fetch the api key.
$config['key_name'] = 'key';
